<nav class="navbar">
  <div class="navbar-container">
    <div class="brand">
      <a href="{{ path('home') }}">
        <img src="{{ asset('images/logo.png') }}" alt="P.NO Logo" class="brand-logo">
        <span>P.NO</span>
      </a>
    </div>
    <div class="nav-links">
      <a href="{{ path('home') }}">Home</a>
      <a href="{{ path('products_index') }}">Shop</a>
      <a href="{{ path('app_brandstory_index') }}">About</a>
      <a href="{{ path('contact') }}">Contact</a>
    </div>
  </div>
</nav>
